@model IEnumerable<ResortManagerSoftware.Models.Education_Degree>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EdName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EdName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EdId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EdId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EdId })
        </td>
    </tr>
}

</table>
