@model IEnumerable<ResortManagerSoftware.Models.ContractDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AttachService.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contract.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AttachService.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contract.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContractDetailId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContractDetailId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContractDetailId })
        </td>
    </tr>
}

</table>
